# Frontend Client Environment Variables

# The base URL of the backend API service
VITE_API_URL="http://localhost:3000/api/v1"

# Google OAuth 2.0 Client ID (public, obtain from Google Developer Console)
# Its "Authorized redirect URIs" must include "<this app's origin>/auth/callback"
VITE_GOOGLE_CLIENT_ID=""
